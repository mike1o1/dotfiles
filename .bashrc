[ -n "$PS1" ] && source ~/.bash_profile

export NVM_DIR="/Users/michaelwilson/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm

export EDITOR=vim

export ANSIBLE_HOSTS=/etc/ansible/hosts
